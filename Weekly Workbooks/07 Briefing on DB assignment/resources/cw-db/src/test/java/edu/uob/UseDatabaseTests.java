package edu.uob;

public class UseDatabaseTests {
    /*
    USE: changes the database against which the following queries will be run
    changing (updating) the ID of a record
    queries on non-existent databases
     */
    // TODO
}
